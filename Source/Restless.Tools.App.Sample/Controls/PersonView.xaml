<UserControl 
    x:Class="Restless.Tools.App.Sample.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="150" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock 
            Foreground="Red"
            FontSize="48"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Text="{Binding LastName}"/>

        <Label Grid.Row="1" Margin="12" HorizontalAlignment="Center" FontSize="16">
            <Hyperlink  ToolTip="This is a test of hyperlink">
                <TextBlock>
                    <Run Text="{Binding FirstName, Mode=OneWay}"/>
                    <Run Text="{Binding LastName, Mode=OneWay}"/>
                </TextBlock>
            </Hyperlink>
        </Label>


    </Grid>
</UserControl>
