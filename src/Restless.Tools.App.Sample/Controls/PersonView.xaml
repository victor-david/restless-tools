<UserControl 
    x:Class="Restless.Tools.App.Sample.PersonView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    d:DesignHeight="70" d:DesignWidth="100">
    <TextBlock FontSize="42" Foreground="Red">
        <Run Text="{Binding FirstName, Mode=OneWay}"/>
        <Run Text="{Binding LastName, Mode=OneWay}"/>
        <Run Foreground="DarkBlue" Text="("/>
        <Run Foreground="DarkBlue" Text="{Binding Position, Mode=OneWay}"/>
        <Run  Foreground="DarkBlue" Text=")"/>
    </TextBlock>
</UserControl>
